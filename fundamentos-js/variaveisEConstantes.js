var a = 3 //Declaração de variável em javascript(var de variavel, a é o nome ou identificador de variável, = recebe e 3 é o valor que ela recebe )
let b = 4//O professor disse para dar-mos preferência pelo let ao invés do var na declaração de variáveis.
/*Devemos dar preferência em usar o let ao invés do var, na declaração de variáveis
 */
var a = 30//Ao rodar o código as variáveis a e b assumiram os valor de 30 e 40, mesmo com os valores 3 e 4 já declarados anteriormente
    b = 40
console.log(a,b)

a = 300
b = 400
console.log(a,b)

/*Quanto mais constantes(const) tiver no programa(aquilo
 *que não precisamos mudar melhor será)
 */
const c = 5